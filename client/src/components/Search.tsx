function Search() {
	return (
		<section className="h-[7%] flex items-center justify-center gap-2">
			<input type="text" placeholder="Current City only" />
			<button>Search</button>
		</section>
	);
}

export default Search;
